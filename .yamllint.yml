---
extends: default

ignore:
  - .venv/
  - .serena/
  - .rtk/
  - output/

rules:
  line-length:
    max: 200
  truthy:
    check-keys: false
  document-start: disable
  empty-lines:
    max: 1
  indentation:
    indent-sequences: whatever
  brackets:
    max-spaces-inside: 1
